import { createTheme, colors } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#ff5722",
        },
        secondary: {
            main: "#CDDC39",
        },
    },
});